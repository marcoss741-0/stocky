interface UpdateProductsContentProps {
  productId: string;
}

const UpdateProductsContent = ({ productId }: UpdateProductsContentProps) => {
  return console.log(productId);
};

export default UpdateProductsContent;
